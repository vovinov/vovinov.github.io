
// Slider - works

$(document).ready(function() {

    // Slider - works
    $('.works__list').slick({
        slidesToShow: 1,
        fade: true,
        autoplay: true,
        autoplaySpeed: 5000
    });
});
